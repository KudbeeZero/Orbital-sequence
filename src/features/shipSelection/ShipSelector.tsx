/**
 * ShipSelector.tsx
 *
 * Ship selection screen component. Provides the full ship selection UI
 * including the 3D carousel preview, statistics comparison, loadout
 * customization, and confirmation flow before entering a mission.
 */

import React from 'react';

export const ShipSelector: React.FC = () => {
  return null;
};

export default ShipSelector;
