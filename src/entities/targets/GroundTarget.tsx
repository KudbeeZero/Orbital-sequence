/**
 * GroundTarget.tsx
 *
 * Surface installation component. Renders destructible ground-based targets
 * such as turrets, generators, and command centers on planetary surfaces.
 * Includes health state visuals, destruction animations, and collision
 * geometry for targeting.
 */

import React from 'react';

export const GroundTarget: React.FC = () => {
  return null;
};

export default GroundTarget;
